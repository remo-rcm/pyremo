name: pyremo-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - cartopy
  - matplotlib-base
  - numpy
  - pooch
  - regionmask
  - setuptools
  - xarray
  - dask
  - cf_xarray
  - xesmf
  - numba
  - netCDF4
# for testing
  - pytest
  - pytest-cov
  - pytest-env
  - fsspec
  - requests
  - aiohttp
  - cmor
  - pip:
    - git+https://github.com/euro-cordex/py-cordex.git # always use master for now
